apiVersion: apps/v1
kind: Deployment
metadata:
  name: bpg-parser-dep
  namespace: ballerina-playground
  labels:
    app: bpg-parser
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bpg-parser
  template:
    metadata:
      labels:
        app: bpg-parser
    spec:
      containers:
      - name: bpg-parser-container
        imagePullPolicy: Always
        image: gcr.io/${BPG_GCP_PROJECT_ID}/parser:v0.1-latest
        # resources:
        #   requests:
        #     cpu: 100m
        #     memory: 100Mi
        ports:
        - name: http-port
          containerPort: 80
        - name: https-port
          containerPort: 443
        readinessProbe:
          tcpSocket:
            port: https-port
          initialDelaySeconds: 3
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: https-port
          initialDelaySeconds: 5
          periodSeconds: 20